You are working inside my Replit project for https://ocmoldpros.com (Next.js site). Ahrefs is reporting 13 internal 404 URLs under /service-areas/*. I want to REMOVE any reference to these locations from my site and ensure the site ONLY contains the 34 approved service-area cities (no other cities should appear anywhere in UI links, SEO links, sitemap, or generated routes).

THE 13 BAD /service-areas/ URLS TO ELIMINATE (currently 404):
- /service-areas/rowland-heights-ca
- /service-areas/cerritos-ca
- /service-areas/long-beach-ca
- /service-areas/rossmoor-ca
- /service-areas/trabuco-canyon-ca
- /service-areas/ladera-ranch-ca
- /service-areas/coto-de-caza-ca
- /service-areas/laguna-woods-ca
- /service-areas/whittier-ca
- /service-areas/la-habra-heights-ca
- /service-areas/north-tustin-ca
- /service-areas/foothill-ranch-ca
- /service-areas/diamond-bar-ca

GOAL
1) Those 13 pages must not be linked anywhere and must not be generated by code.
2) The site must only list/link to my approved 34 service-area cities.
3) Add a safety net so if any of those 13 URLs are requested, they do NOT return a 404.
   - Preferred: 301 redirect them to /service-areas (the main service areas page) OR to the closest existing city page if one clearly exists.
4) Regenerate sitemap so it contains ONLY valid indexable pages (and definitely not those 13).
5) After changes, do a quick internal link scan to confirm there are no references to those 13 slugs.

IMPORTANT: I will paste my approved list of 34 cities when you locate where the site stores the service areas list.
- First, find the “single source of truth” for service areas (a data file/array/json/cms mock) and show me the file + the current list it contains.
- Then replace that list with a new constant called APPROVED_SERVICE_AREAS that will contain ONLY the 34 approved entries.
- Make the rest of the app reference ONLY that constant, so we never accidentally link to non-approved cities again.

WHAT TO DO (step-by-step)
A) Locate service-area data + routes
- Search the codebase for: “service-areas”, “/service-areas/”, “serviceAreas”, “cities”, and specifically for each of these slugs:
  rowland-heights, cerritos, long-beach, rossmoor, trabuco-canyon, ladera-ranch, coto-de-caza, laguna-woods, whittier, la-habra-heights, north-tustin, foothill-ranch, diamond-bar.
- Identify where service area pages are generated:
  - If App Router: app/service-areas/[slug]/page.tsx and generateStaticParams()
  - If Pages Router: pages/service-areas/[slug].tsx and getStaticPaths()
  - Or any custom generator.

B) Enforce “only 34 cities”
- Create/replace a central constant (e.g. in /lib/serviceAreas.ts or /data/service-areas.ts):
  export const APPROVED_SERVICE_AREAS = [
    // { name, slug, county?, state? }, exactly the 34 I approve
  ];
- Update all components to use APPROVED_SERVICE_AREAS for:
  - service areas index page list
  - header/footer/nav links
  - internal CTAs (“Areas we serve”, etc.)
  - schema/JSON-LD location lists
  - any “related cities” widgets
  - any sitemap generator inputs

C) Remove any references to the 13 bad cities
- Delete them from any data arrays, hardcoded links, or “popular cities” sections.
- Ensure they are not included in generateStaticParams/getStaticPaths so they can’t be built.

D) Add redirects so those 13 URLs never 404
- Implement 301 redirects in the appropriate place:
  - Next.js: next.config.js/ts redirects() OR middleware.ts
- Map each of the 13 bad URLs → /service-areas (or a specific existing city page if appropriate).
- Confirm redirect status is 301 (not 302).

E) Fix sitemap
- If sitemap is dynamic (next-sitemap or custom route), ensure it only includes:
  - static pages
  - approved service areas from APPROVED_SERVICE_AREAS
- Ensure removed cities are not present.

F) Verification (must do)
- Run:
  - npm run build (or pnpm build/yarn build depending on repo)
  - npm run lint (if present)
- Then verify locally (or via simple node script) that:
  1) No file contains any of the 13 slugs anymore (except in the redirect map).
  2) All service area links rendered are from APPROVED_SERVICE_AREAS only.
  3) Visiting any of the 13 URLs returns a 301 to /service-areas.
  4) Sitemap does not include those 13 URLs.

DELIVERABLES
- Tell me which file is now the “source of truth” for service areas.
- Show me the redirect implementation file and the final redirect map.
- Confirm the checks in (F) passed.

DO NOT
- Do not add new cities beyond my 34.
- Do not keep “nearby locations” lists that reintroduce removed cities.
- Do not leave those 13 URLs as 404.

Start by finding and showing the current service areas data list, then pause and ask me to paste the approved 34-city list so you can replace it cleanly.
